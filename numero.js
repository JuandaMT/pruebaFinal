const numeroDado = prompt("introduzca un numero")
parseInt(numeroDado)
const numeroLimite = numeroDado + 50;

console.log(numeroLimite)

/* for(let i = numeroDado; i <= numeroLimite; i++){
    if(i%2 !== 0){
        console.log(i);
    }
} */